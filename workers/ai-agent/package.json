{
  "name": "ai-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Pimenta Lab chat agent on Cloudflare Workers \u2014 an MCP client that runs a Workers AI tool-calling loop against the pimenta-ai-mcp server.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.36.0"
  }
}
